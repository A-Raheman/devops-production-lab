events {}

http {
 upstream flask_upstream {
  server 172.21.0.2:5000;
  server 172.21.0.3:5000;
 }

  server {
    listen 80;
    
    location / {
      proxy_pass http://flask_upstream;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
     }
   }
}
